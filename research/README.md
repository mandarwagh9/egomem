# research/

Phase 1 (RESEARCH) artifacts. Expected files, in order of creation:

1. `landscape.md` — lit map: a sources table covering Embodied VideoAgent /
   persistent scene memory, memory-in-VLA, memory-in-world-model, and
   egocentric LeRobot-v3 datasets. For each: memory mechanism, model-bound vs
   model-agnostic, and substrate.
2. `hypothesis.md` — the ONE falsifiable hypothesis: named metric + threshold +
   exactly what observation would falsify it. (Phase 1 EXIT.)
3. `design.md` — Phase 2: neutral API signatures, the data contract both a
   world model and a VLA consume, the dataset, the baselines, and the single
   experiment that tests the hypothesis. (Phase 2 EXIT.)

Every source referenced here must also appear, verified with a link, in
`../BIBLIOGRAPHY.md`.
