# lib/

The eventual EgoMem library + CLI (Phase 5, BUILD).

The validated method from Phases 3–4 gets turned into a clean, installable
package here: real install, real demo on real/sample data, honest error
handling, no dead code, no stubs presented as working.

Do not pre-build this. It stays empty until there is a defended result to
package.
